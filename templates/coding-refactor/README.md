# Coding refactor template

Copy this folder when a project needs a tailored refactor workflow.

Keep the baseline step unless the target project cannot run checks before mutation. If there is no baseline, document why in the flow README.

