# Coding feature template

Copy this folder when a project needs a customized feature workflow.

Replace:

- `id`
- `runtime.supported_cores`
- `runtime.required_capabilities`
- quality gate commands
- node descriptions that are project-specific

