# Compare required against supported

Match every required capability against the runtime profile and record each as supported or unsupported in the capability matrix, with no requirement left unjudged.
